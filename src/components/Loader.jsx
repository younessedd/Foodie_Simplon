import React from "react";

const Loader = () => (
  <div className="loader">
    {/* Spinner animation */}
    <div className="spinner" />

    {/* Loading text */}
    <span>Loading...</span>
  </div>
);

export default Loader;
